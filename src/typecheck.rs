use crate::ast::*;

pub fn typecheck(_stmt: &Stmt) -> Result<(), String> {
    // TODO: Implement minimal type checks
    // For example, ensure that arithmetic operations are on integers
    Ok(())
}
